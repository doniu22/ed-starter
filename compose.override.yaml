services:
  app:
    build:
      target: dev
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development

  db:
    ports:
      - "5432:5432"
    healthcheck:
      interval: 5s